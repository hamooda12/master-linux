# Linux Practice

> A hands-on Linux learning repository documenting my journey from Linux fundamentals to advanced system administration and DevOps.

## Completed Chapters

### Chapter 01 – Linux Filesystem & File Management
- Navigation (`pwd`, `cd`)
- Listing files (`ls`)
- Creating files/directories (`touch`, `mkdir`)
- File operations (`cp`, `mv`, `rm`, `rmdir`)
- File information (`file`, `stat`)
- Permissions & ownership (`chmod`, `chown`, `chgrp`)
- Hard & symbolic links
- SUID, SGID, Sticky Bit
- Linux Filesystem Hierarchy (FHS)

### Chapter 02 – Working with Text Files and Streams
- Viewing files (`cat`, `more`, `less`)
- Reading large files (`head`, `head -n`, `tail`, `tail -n`, `tail -f`)
- Searching (`grep` + regex basics + common options)
- Counting (`wc`)
- Sorting (`sort`, `uniq`, `sort -k`, `sort -u`, `sort -h`)
- Comparing (`diff`, `diff -u`, `cmp`)
- Redirection (`>`, `>>`, `<`, `2>`, `2>>`, `2>&1`, `&>`, `/dev/null`, `tee`)
- Pipes and Pipelines
- Log Analysis Project

## Progress

- ✅ Chapter 01 Complete
- ✅ Chapter 02 Complete
- ⏳ Chapter 03 – Archives, Compression & Backups
